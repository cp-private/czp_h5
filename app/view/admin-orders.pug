<!DOCTYPE html>
html
    head
        title 订单管理
        meta(charset="utf-8")
        link(href="https://cdn.bootcss.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous")
    body
        h2 订单管理
        select(id="agentFilter")
            option(value="请选择") 请选择
            each j in agents
                option(value=`${j.name}`) #{j.name}
        .panel.panel-default
            table.table
                tr 
                    th 姓名
                    th 电话
                    th 地址
                    th 面积(平米)
                    th 代理商
                    th 下单时间
                    th 下单地址 
                each i in list
                    tr 
                        td #{i.name}
                        td #{i.tel}
                        td #{i.where}
                        td #{i.area}
                        td #{i.agent}
                        td #{i.create_date}
                        td #{i.addr}
        script(src="http://apps.bdimg.com/libs/jquery/2.1.4/jquery.min.js")
        script(src="https://cdn.bootcss.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous")
        script(type="text/javascript") $('#agentFilter').on('change', function() {location.href="http://order.rudolf1922.com/admin/__orders__?agent=" + $('#agentFilter').val()})

    

